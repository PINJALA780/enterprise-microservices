apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: enterprise

spec:
  replicas: 1

  selector:
    matchLabels:
      app: notification-service

  template:
    metadata:
      labels:
        app: notification-service

    spec:
      containers:
        - name: notification-service

          image: venkatesh2007567/notification-service:1.0.0
          imagePullPolicy: Always

          ports:
            - containerPort: 8085
              protocol: TCP

          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD

            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_DB

          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi

---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: enterprise

spec:
  type: ClusterIP

  selector:
    app: notification-service

  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
